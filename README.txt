Queue push / pop java exercise.

Queue uses an array behind the scenes and two methods: a push method to add to queue and a pop method to remove from queue.

Amreen created the push method
Kilmar created the pop method